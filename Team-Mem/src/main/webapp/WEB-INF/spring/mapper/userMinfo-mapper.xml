<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.callor.hello.dao.UserMinfoDao">

	<select id="selectAll" >
		SELECT * FROM tbl_minfo 
		WHERE i_ccode = #{ccode}
	</select>

	<insert id="insert">
		INSERT INTO tbl_user_minfo(r_iseq,r_uid, r_icount, r_sdate, r_edate)
		VALUES(#{r_iseq}, #{us_uid}, #{r_icount}, #{r_sdate}, #{r_edate} )
	</insert>
	
	<select id="findById">
		SELECT * FROM tbl_user_minfo
		JOIN tbl_minfo ON r_iseq = i_seq
		WHERE r_uid = #{seq}
	</select>
	
	<select id="findBySeq">
		SELECT * FROM tbl_minfo
		WHERE i_seq = #{seq}
	</select>

	<update id="update" >
    	<foreach collection="vo" item="vo" separator=";">
        	UPDATE tbl_user_minfo
        	SET
        	r_icount = #{vo.r_icount},
        	r_sdate = #{vo.r_sdate},
        	r_edate = #{vo.r_edate}
        	WHERE r_iseq = #{vo.r_iseq}
    	</foreach>
	</update>

	<delete id="delete">
		DELETE FROM tbl_user_minfo WHERE r_uid = #{seq}
	</delete>

</mapper> 