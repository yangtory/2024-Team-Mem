<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace="com.callor.hello.dao.TeacherDao">
 
<!--  	<select id="selectAll">
 		SELECT * FROM tbl_teacher 
 		WHERE t_ccode = #{comp}
 	</select> -->
 
 	<select id="selectSearchAll">
 		SELECT * FROM tbl_teacher
 		<where>
 			<if test="t_name != null and t_name != '' ">
 			AND t_name LIKE CONCAT('%', #{t_name}, '%')</if>
 			<if test="t_code!= null and t_code != '' ">
 			AND t_code LIKE CONCAT('%', #{t_code}, '%')</if>
 			<if test="t_tel!= null and t_tel != '' ">
 			AND t_tel LIKE CONCAT('%', #{t_tel}, '%')</if>
 			AND t_ccode = #{comp}
 		</where>
 	</select>
 	
 	<insert id="insert">
 		INSERT INTO tbl_teacher(t_code, t_name, t_tel, t_ccode)
 		VALUES(#{t_code}, #{t_name}, #{t_tel}, #{t_ccode})
 	</insert>
 	
 	<select id="findByComp">
 		SELECT c_code FROM tbl_company
 		WHERE c_name = #{ucomp}
 	</select>
 	<update id="update"></update>
 	<delete id="delete"></delete>
 
 </mapper> 