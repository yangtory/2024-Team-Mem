<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.step13.mapper.BoardMapper">

  <!-- board 테이블의 모든 레코드를 조회 -->
  <select id="getList" resultType="com.example.step13.domain.BoardVO">
    <![CDATA[
    SELECT * FROM board WHERE id > 0
    ]]>
  </select>

  <!-- Criteria 객체로 페이징 처리 -->
  <select id="getListWithPaging" resultType="com.example.step13.domain.BoardVO">
    <![CDATA[
    SELECT id, title, writer, regdate, updatedate
    FROM board
    ORDER BY id DESC
    OFFSET (#{page} - 1) * #{rowsPerPage} ROWS FETCH FIRST #{rowsPerPage} ROWS ONLY
    ]]>
  </select>

  <!-- CREATE: 새로운 게시물을 등록(추가한 게시물의 id를 알 필요가 없는 경우) -->
  <insert id="insert">
    INSERT INTO board (id, title, content, writer)
    VALUES (NEXTVAL('SEQ_BOARD_ID'), #{title}, #{content}, #{writer})
  </insert>

  <!-- CREATE: 새로운 게시물을 등록(추가한 게시물의 id를 알아야 하는 경우) -->
  <insert id="insertSelectKey">
    <selectKey keyProperty="id" order="BEFORE" resultType="long">
      SELECT NEXTVAL('SEQ_BOARD_ID') FROM dual
    </selectKey>

    INSERT INTO board (id, title, content, writer)
    VALUES (#{id}, #{title}, #{content}, #{writer})
  </insert>

  <!-- READ: PK인 id 컬럼으로 특정 게시물을 조회 -->
  <select id="read" resultType="com.example.step13.domain.BoardVO">
    SELECT * FROM board WHERE id = #{id}
  </select>

  <!-- UPDATE: PK인 id 컬럼으로 특정 게시물을 갱신 -->
  <update id="update">
    UPDATE board
    SET title = #{title}, content = #{content}, writer = #{writer},
      updatedate = CURRENT_TIMESTAMP
    WHERE id = #{id}
  </update>

  <!-- DELETE: PK인 id 컬럼으로 특정 게시물을 삭제 -->
  <delete id="delete">
    DELETE FROM board WHERE id = #{id}
  </delete>

  <!-- 전체 게시물의 개수를 조회 -->
  <select id="getTotal" resultType="long">
    SELECT COUNT(*) FROM board WHERE id > 0
  </select>
</mapper>
